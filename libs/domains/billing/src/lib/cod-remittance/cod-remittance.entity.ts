/**
 * SS-033 — CodRemittanceEntity TypeORM entity.
 *
 * This file is a re-export of the canonical entity definition in
 * `@swiftship/platform-typeorm` (billing.entities.ts). The cod-remittance
 * module imports from here so the public API is self-contained.
 */
export { CodRemittanceEntity, CodDisputeEntity } from '@swiftship/platform-typeorm';
