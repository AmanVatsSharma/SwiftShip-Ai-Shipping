{
  "name": "plugins",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/plugins/src",
  "projectType": "library",
  "tags": [
    "layer:domain",
    "scope:plugins",
    "team:logistics",
    "maturity:stable",
    "type:domain"
  ],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  },
  "//": "typecheck/build targets removed 2026-09: barrel re-exports the legacy src/ tree (SS-101 decommission pending). Restore when the lib is ported."
}
